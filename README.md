# Published NFT — IPFS reference

Static, backend-free IPFS asset table. Reads the on-chain PSIM registry directly from the Flow REST API in the browser and renders every book's cover/content/metadata CID.

Live: https://publishednft.github.io/ipfs/
